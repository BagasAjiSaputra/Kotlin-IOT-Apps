<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HydroSense">

        <!-- Splash Activity -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.App.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.HydroSense" />

        <!-- 6 Activity -->
        <activity android:name=".ui.screen.login.LoginActivity" />
        <activity android:name=".ui.screen.home.HomeActivity" />
        <activity android:name=".ui.screen.history.HistoryActivity" />
        <activity android:name=".ui.screen.mode.ModeActivity" />
        <activity android:name=".ui.screen.chart.ChartActivity" />
        <activity android:name=".ui.screen.setting.SettingActivity" />
        <activity android:name=".ui.screen.test.TestActivity" />


    </application>

</manifest>
